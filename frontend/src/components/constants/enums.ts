export enum AspectRatio {
  square = 1,
  banner = 16 / 9,
  free = 0,
}

export enum RestaurantType {
  fastFood = 'Comida rápida',
  restaurant = 'Restaurante',
  bar = 'Bar',
  pub = 'Pub',
  cafe = 'Cafetería',
  other = 'Otro',
}